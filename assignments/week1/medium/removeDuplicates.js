/*
  Write a function `removeDuplicates` that takes an array as a parameter 
  and returns a new array with duplicate elements removed.
*/

function removeDuplicates(arr) {
    // Your code here
}

const originalArray = [1, 2, 3, 4, 4, 5, 6, 6, 7];
const uniqueArray = removeDuplicates(originalArray);

console.log(uniqueArray);  // Expected output: [1, 2, 3, 4, 5, 6, 7]
